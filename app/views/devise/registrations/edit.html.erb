<h2>Einstellungen </h2>

<%= form_for(resource_name, resource, :url => registration_path(resource_name), :html => { :method => :put }) do |f| %>
 

  <p><%= f.label :Email %></p>
  <p><%= f.text_field :email %></p>

  <p><%= f.label :Passwort %> <i>(Einfach leer lassen falls Sie es nicht &auml;ndern wollen.)</i></p>
  <p><%= f.password_field :password %></p>

  <p><%= f.label :Passwort_wiederholen %></p>
  <p><%= f.password_field :password_confirmation %></p>

  <p><%= f.label :aktuelles_Passwort %> <i>(Aus Sicherheitsgr&uuml;nden muss dies bei &Auml;nderungen eingegeben werden.)</i></p>
  <p><%= f.password_field :current_password %></p>

  <p><%= f.submit "&Auml;nderung speichern" %></p>
<% end %>
