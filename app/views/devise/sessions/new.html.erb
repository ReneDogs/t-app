<h2>Einloggen</h2>

<%= form_for(resource_name, resource, :url => session_path(resource_name)) do |f| %>
  <p><%= f.label :E_mail %></p>
  <p><%= f.text_field :email %></p>

  <p><%= f.label :Passwort %></p>
  <p><%= f.password_field :password %></p>
  
  <p><%= f.submit "einloggen !" %></p>
<% end %>

<%= render :partial => "devise/shared/links" %>